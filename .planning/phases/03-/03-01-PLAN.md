---
phase: 03-admin-panel-core
plan: 01
type: execute
wave: 1
depends_on: ["01-authentication-foundation", "02-database-security-performance"]
files_modified:
  - client/src/components/admin/UserStatusBadge.tsx
  - client/src/components/admin/RevokeConfirmDialog.tsx
  - client/src/components/admin/UserListTable.tsx
  - client/src/pages/AdminPanel.tsx
  - server/routes/admin-users.ts
  - server/routes/admin-access.ts
  - server/middleware/admin-auth.ts
  - server/lib/audit-logger.ts
  - client/src/hooks/useAdminUsers.ts
  - client/src/lib/validation/admin.ts
  - client/src/components/admin/__tests__/UserStatusBadge.test.tsx
  - client/src/components/admin/__tests__/RevokeConfirmDialog.test.tsx
  - server/routes/__tests__/admin-access.test.ts
  - server/routes/__tests__/admin-users.test.ts
  - e2e/admin-panel.spec.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-05
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
  - ADMIN-04
  - ADMIN-07
user_setup: []
must_haves:
  truths:
    - Admin can view list of all users with color-coded status badges (green=active, yellow=pending, red=expired, blue=manual)
    - Admin can grant manual access to any user with optional reason and expiration date
    - Admin can revoke access (manual or paid) with confirmation dialog before destructive action
    - System records every grant/revoke operation with timestamp, admin user ID, and reason
    - Admin operations validate admin role on server (service role) before allowing changes
  artifacts:
    - path: "client/src/components/admin/UserStatusBadge.tsx"
      provides: "Color-coded status badge component"
      min_lines: 60
      exports: ["UserStatusBadge"]
    - path: "client/src/components/admin/RevokeConfirmDialog.tsx"
      provides: "Confirmation dialog for destructive actions"
      min_lines: 50
      exports: ["RevokeConfirmDialog"]
    - path: "client/src/components/admin/UserListTable.tsx"
      provides: "User listing table with status badges"
      min_lines: 120
      exports: ["UserListTable"]
    - path: "client/src/pages/AdminPanel.tsx"
      provides: "Main admin panel page"
      min_lines: 200
      exports: ["default"]
    - path: "server/routes/admin-users.ts"
      provides: "User list API with access status"
      min_lines: 80
      exports: ["adminUsersRouter"]
    - path: "server/routes/admin-access.ts"
      provides: "Enhanced admin access API with audit trail"
      min_lines: 200
      exports: ["adminAccessRouter"]
    - path: "server/middleware/admin-auth.ts"
      provides: "Reusable admin authentication middleware"
      min_lines: 40
      exports: ["verifyAdmin"]
    - path: "server/lib/audit-logger.ts"
      provides: "Centralized audit logging utility"
      min_lines: 30
      exports: ["logAdminAction"]
  key_links:
    - from: "client/src/pages/AdminPanel.tsx"
      to: "/api/admin/users"
      via: "useAdminUsers hook"
      pattern: "fetch.*api/admin/users"
    - from: "client/src/pages/AdminPanel.tsx"
      to: "UserStatusBadge"
      via: "import in UserListTable"
      pattern: "import.*UserStatusBadge"
    - from: "client/src/components/admin/RevokeConfirmDialog.tsx"
      to: "/api/admin/access/:userId"
      via: "DELETE call on confirm"
      pattern: "DELETE.*api/admin/access"
    - from: "server/routes/admin-access.ts"
      to: "user_manual_access"
      via: "soft delete with audit fields"
      pattern: "is_active.*false.*revoked_by"
    - from: "server/routes/admin-users.ts"
      to: "server/middleware/admin-auth.ts"
      via: "verifyAdmin middleware"
      pattern: "verifyAdmin"
---

## Objective

Build a comprehensive admin panel that enables secure user access management with proper audit trails, server-side authorization, and confirmation dialogs for destructive actions.

**Purpose:** Provide admins with a secure interface to view all users, grant manual access with optional expiration, and revoke access while maintaining complete audit history of all operations.

**Output:** Working admin panel at `/admin` with user listing, color-coded status badges, grant/revoke functionality, and complete audit trail logging.

## Context

@.planning/phases/03-/03-RESEARCH.md
@.planning/phases/03-/03-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@client/src/pages/AdminAccess.tsx
@server/routes/admin-access.ts
@client/src/components/ui/badge.tsx
@client/src/components/ui/alert-dialog.tsx
@client/src/components/ui/table.tsx

## Tasks

### Task 1: Create Wave 0 test infrastructure

<task type="auto">
  <name>Task 1.1: Create test files for admin components and APIs</name>
  <files>
    client/src/components/admin/__tests__/UserStatusBadge.test.tsx
    client/src/components/admin/__tests__/RevokeConfirmDialog.test.tsx
    server/routes/__tests__/admin-access.test.ts
    server/routes/__tests__/admin-users.test.ts
    e2e/admin-panel.spec.ts
  </files>
  <action>
    Create test scaffold files for all admin functionality:

    1. **UserStatusBadge.test.tsx**: Unit tests for badge color variants
       - Test "active" status renders green badge with checkmark
       - Test "pending" status renders yellow badge with clock icon
       - Test "expired" status renders red badge with X icon
       - Test "manual" status renders blue badge with shield icon
       - Test icon rendering with correct lucide-react icons
       - Test label text matches status

    2. **RevokeConfirmDialog.test.tsx**: Component tests for dialog behavior
       - Test dialog opens when trigger clicked
       - Test dialog shows user name and email in description
       - Test dialog closes when cancel clicked without calling onConfirm
       - Test dialog calls onConfirm and closes when confirm clicked
       - Test AlertDialogAction has destructive styling

    3. **admin-access.test.ts**: Integration tests for admin access API
       - Test verifyAdmin middleware blocks requests without Bearer token
       - Test verifyAdmin middleware blocks non-admin users (403)
       - Test POST /grant creates user_manual_access record with correct fields
       - Test POST /grant returns 409 if user already has active manual access
       - Test DELETE /:userId sets is_active to false (soft delete)
       - Test DELETE /:userId sets revoked_at, revoked_by, revoke_reason fields
       - Test audit log entry created for revoke operation

    4. **admin-users.test.ts**: Integration tests for user list API
       - Test GET / returns all users with enriched status field
       - Test status is "manual" when user has active manual access
       - Test status is "active" when user has active payment access
       - Test status is "expired" when user's access has expired
       - Test status is "free" when user has no access
       - Test endpoint returns 401 without auth token
       - Test endpoint returns 403 for non-admin users

    5. **admin-panel.spec.ts**: E2E tests for complete admin workflows
       - Test admin can view list of all users
       - Test status badges are visible with correct colors
       - Test grant access flow: fill form, submit, verify success toast
       - Test grant access with optional reason and expiration date
       - Test revoke access flow: click button, confirm dialog, verify success
       - Test revoke confirmation dialog shows warning message
       - Test cancel button in dialog does not revoke access

    Use existing test patterns from client/src/__tests__/setup.ts
    Mock Supabase admin client using vitest fixtures
  </action>
  <verify>
    <automated>pnpm test -- admin 2>/dev/null || echo "Tests scaffolded but will fail until implementation"</automated>
  </verify>
  <done>
    All test files created with failing tests (RED state)
    Test files follow existing project patterns
    Mock fixtures configured for Supabase admin client
  </done>
</task>

### Task 2: Create reusable admin authentication middleware

<task type="auto">
  <name>Task 2: Extract admin authentication middleware</name>
  <files>
    server/middleware/admin-auth.ts
  </files>
  <action>
    Extract the verifyAdmin function from server/routes/admin-access.ts into a reusable middleware module:

    1. Create server/middleware/admin-auth.ts with:
       - Import createClient from @supabase/supabase-js
       - Initialize supabaseAdmin with service role key (reuse existing pattern)
       - Export verifyAdmin function that:
         * Checks for Authorization header with "Bearer " prefix
         * Calls supabaseAdmin.auth.getUser(token) to validate token
         * Checks user.user_metadata.role === "admin"
         * Attaches user object to request: (req as any).user = user
         * Returns 401 if no token or invalid token
         * Returns 403 if token valid but user not admin
         * Calls next() if all checks pass

    2. Error handling:
       - Wrap in try-catch
       - Log errors to console.error for debugging
       - Return 500 for unexpected errors

    Follow the existing pattern from admin-access.ts lines 24-51, but make it reusable across routes.
  </action>
  <verify>
    <automated>grep -r "verifyAdmin" server/middleware/admin-auth.ts | head -5</automated>
  </verify>
  <done>
    verifyAdmin function exported from server/middleware/admin-auth.ts
    Function validates Bearer token and admin role
    Function attaches user to request object
    Error handling implemented with proper status codes
  </done>
</task>

### Task 3: Create audit logging utility

<task type="auto">
  <name>Task 3: Create centralized audit logging utility</name>
  <files>
    server/lib/audit-logger.ts
  </files>
  <action>
    Create a centralized audit logging utility to track all admin operations:

    1. Create server/lib/audit-logger.ts with logAdminAction function:
       - Accepts parameters: action (string), targetUserId (string), adminUserId (string), metadata (object)
       - Inserts record into admin_audit_log table (create if doesn't exist)
       - Fields: id (uuid), action, target_user_id, admin_user_id, timestamp (now), metadata (jsonb)
       - Returns the created audit record

    2. Create migration file for admin_audit_log table if needed:
       - supabase/migrations/007_admin_audit_log.sql
       - CREATE TABLE with: id UUID PRIMARY KEY, action TEXT, target_user_id UUID, admin_user_id UUID, timestamp TIMESTAMPTZ, metadata JSONB
       - RLS policy: Service role can insert, admins can read

    3. Export logAdminAction as default export

    Use Supabase admin client for database insertion to bypass RLS.
  </action>
  <verify>
    <automated>grep -r "logAdminAction" server/lib/audit-logger.ts | head -3</automated>
  </verify>
  <done>
    logAdminAction function created in server/lib/audit-logger.ts
    Function inserts into admin_audit_log table
    Migration file created for table schema
    Function returns created audit record
  </done>
</task>

### Task 4: Create UserStatusBadge component

<task type="auto" tdd="true">
  <name>Task 4: Create UserStatusBadge component</name>
  <files>
    client/src/components/admin/UserStatusBadge.tsx
  </files>
  <behavior>
    - Renders green badge with checkmark icon for "active" status
    - Renders yellow badge with clock icon for "pending" status
    - Renders red badge with X icon for "expired" status
    - Renders blue badge with shield icon for "manual" status
    - Badge label matches status name in Portuguese
    - Icon positioned before text with gap
  </behavior>
  <action>
    Create UserStatusBadge component following the research pattern:

    1. Define UserStatus type: "active" | "pending" | "expired" | "manual" | "free"

    2. Create statusConfig object mapping each status to:
       - variant: "default" | "secondary" | "destructive" | "outline"
       - icon: Lucide React icon component (CheckCircle, Clock, XCircle, Shield, User)
       - label: Portuguese text ("Ativo", "Pendente", "Expirado", "Manual", "Grátis")
       - className: Custom color overrides (green, yellow, blue for proper colors)

    3. Component props: status (UserStatus)

    4. Render Badge component with:
       - variant from config
       - custom className for color (use inline colors to ensure correctness)
       - icon and label as children

    5. Color mapping per requirements:
       - active: green (#10b981)
       - pending: yellow (#f59e0b)
       - expired: red (destructive variant)
       - manual: blue (#3b82f6)
       - free: gray/secondary

    Import Badge from @/components/ui/badge and icons from lucide-react.
  </action>
  <verify>
    <automated>pnpm test -- UserStatusBadge.test.tsx</automated>
  </verify>
  <done>
    UserStatusBadge component renders correct colors for all status types
    Icons display correctly for each status
    Labels are in Portuguese
    Component exported from client/src/components/admin/UserStatusBadge.tsx
    All tests passing
  </done>
</task>

### Task 5: Create RevokeConfirmDialog component

<task type="auto" tdd="true">
  <name>Task 5: Create RevokeConfirmDialog component</name>
  <files>
    client/src/components/admin/RevokeConfirmDialog.tsx
  </files>
  <behavior>
    - Opens dialog when trigger button clicked
    - Displays user email in warning message
    - Shows warning that action cannot be undone
    - Calls onConfirm callback when confirmed
    - Closes dialog after confirmation
    - Does NOT call onConfirm when cancelled
  </behavior>
  <action>
    Create RevokeConfirmDialog component using Radix UI AlertDialog:

    1. Import all AlertDialog components from @/components/ui/alert-dialog

    2. Props interface:
       - userName: string
       - userEmail: string
       - onConfirm: () => Promise<void>

    3. Component state: open (boolean) for controlled dialog

    4. Handle confirm function:
       - Await onConfirm()
       - Set open(false) to close dialog
       - Handle errors gracefully (let caller handle with toast)

    5. Render:
       - AlertDialog with open/onOpenChange
       - AlertDialogTrigger as Button with UserX icon (ghost variant, small)
       - AlertDialogContent with:
         * AlertDialogTitle: "Revogar acesso de {userName}?"
         * AlertDialogDescription: Warning message with userEmail, "Esta ação não pode ser desfeita"
         * AlertDialogFooter with:
           - AlertDialogCancel: "Cancelar"
           - AlertDialogAction: "Revogar acesso" with destructive styling (bg-destructive)

    Follow the pattern from research section lines 152-205.
  </action>
  <verify>
    <automated>pnpm test -- RevokeConfirmDialog.test.tsx</automated>
  </verify>
  <done>
    RevokeConfirmDialog component renders confirmation dialog
    Dialog shows user email in warning message
    Confirm button has destructive styling
    Component exported from client/src/components/admin/RevokeConfirmDialog.tsx
    All tests passing
  </done>
</task>

### Task 6: Create admin-users API route

<task type="auto" tdd="true">
  <name>Task 6: Create admin-users API route</name>
  <files>
    server/routes/admin-users.ts
  </files>
  <behavior>
    - Returns 401 if no Authorization header
    - Returns 403 if user is not admin
    - Returns list of all users with enriched status field
    - Status is "manual" when user has active manual access
    - Status is "active" when user has active payment access
    - Status is "expired" when access has passed expires_at
    - Status is "free" when user has no access
  </behavior>
  <action>
    Create new API route for listing all users with access status:

    1. Create server/routes/admin-users.ts:
       - Import Router, createClient from @supabase/supabase-js
       - Import verifyAdmin from ../middleware/admin-auth
       - Initialize supabaseAdmin with service role
       - Export adminUsersRouter

    2. Create GET / endpoint with verifyAdmin middleware:
       - Fetch all auth users: supabaseAdmin.auth.admin.listUsers()
       - Fetch all manual access: from user_manual_access select("*")
       - Fetch all payment access: from user_access select("*")
       - Enrich each user with status calculation

    3. Status calculation logic (in order of priority):
       - If has expired access (manual or payment): status = "expired"
       - If has active manual access: status = "manual"
       - If has active payment access: status = "active"
       - Otherwise: status = "free"

    4. Check for expired access:
       - Compare expires_at with new Date()
       - Handle null expires_at as "no expiration"

    5. Response format:
       ```json
       {
         "users": [
           {
             "id": "uuid",
             "email": "user@example.com",
             "created_at": "2024-01-01T00:00:00Z",
             "status": "active" | "manual" | "expired" | "free",
             "manual_access": { ... } | null,
             "payment_access": { ... } | null
           }
         ]
       }
       ```

    6. Error handling:
       - 500 on database errors
       - Log errors to console.error

    Follow the pattern from research section lines 440-517.
  </action>
  <verify>
    <automated>pnpm test -- admin-users.test.ts</automated>
  </verify>
  <done>
    GET /api/admin/users returns all users with status
    Status calculation follows priority order correctly
    Endpoint protected by verifyAdmin middleware
    Response format matches specification
    All tests passing
  </done>
</task>

### Task 7: Enhance admin-access route with audit trail

<task type="auto" tdd="true">
  <name>Task 7: Enhance admin-access route with audit trail</name>
  <files>
    server/routes/admin-access.ts
  </files>
  <behavior>
    - DELETE /:userId sets is_active to false
    - DELETE sets revoked_at, revoked_by, revoke_reason fields
    - Audit log entry created for revoke operation
    - Reactivate endpoint preserves original granted_by, granted_at
    - All operations use verifyAdmin middleware
  </behavior>
  <action>
    Enhance existing admin-access.ts route with proper audit trail:

    1. Replace inline verifyAdmin with import from ../middleware/admin-auth

    2. Add Zod validation schemas:
       - grantAccessSchema: { email: string.email(), reason: string().optional(), expires_at: string().datetime().nullable().optional() }
       - revokeAccessSchema: { reason: string().optional() }

    3. Update DELETE /:userId endpoint:
       - Accept optional reason in request body
       - Update record with: is_active: false, revoked_at: now(), revoked_by: adminUser.id, revoke_reason: reason
       - Call logAdminAction with action: "revoke_manual_access"
       - Return 404 if no active access found

    4. Update POST /grant endpoint:
       - Add Zod validation using grantAccessSchema
       - Return 400 if validation fails with error details
       - Call logAdminAction with action: "grant_manual_access"
       - Include reason and expires_at in metadata

    5. Update POST /:userId/reactivate endpoint:
       - Call logAdminAction with action: "reactivate_manual_access"
       - Preserve original granted_by and granted_at fields

    6. Import logAdminAction from ../lib/audit-logger

    Keep existing functionality for GET /list, GET /check/:email endpoints.

    Follow soft delete pattern from research section lines 270-297.
  </action>
  <verify>
    <automated>pnpm test -- admin-access.test.ts</automated>
  </verify>
  <done>
    DELETE endpoint uses soft delete with audit fields
    All admin operations logged to admin_audit_log table
    Zod validation schemas applied to grant/revoke
    verifyAdmin middleware imported from shared module
    All tests passing
  </done>
</task>

### Task 8: Create useAdminUsers hook

<task type="auto">
  <name>Task 8: Create useAdminUsers hook</name>
  <files>
    client/src/hooks/useAdminUsers.ts
  </files>
  <action>
    Create custom React hook for fetching admin user list:

    1. Create client/src/hooks/useAdminUsers.ts

    2. Hook signature: useAdminUsers() -> { users, loading, error, refetch }

    3. State management:
       - users: AdminUser[] (initialized to [])
       - loading: boolean (initialized to true)
       - error: string | null (initialized to null)

    4. Fetch function:
       - GET /api/admin/users with Authorization header
       - Get token from supabase.auth.session()
       - Parse response JSON
       - Set users or error based on response
       - Set loading to false

    5. Refetch function:
       - Sets loading to true
       - Calls fetch function again

    6. useEffect to fetch on mount
    7. Return { users, loading, error, refetch }

    Type definition for AdminUser:
    ```typescript
    interface AdminUser {
      id: string;
      email: string;
      created_at: string;
      status: "active" | "pending" | "expired" | "manual" | "free";
      manual_access: any | null;
      payment_access: any | null;
    }
    ```

    Import useAuth from @/contexts/AuthContext to get session token.
  </action>
  <verify>
    <automated>grep -r "useAdminUsers" client/src/hooks/useAdminUsers.ts | head -3</automated>
  </verify>
  <done>
    useAdminUsers hook created with fetch, loading, error states
    Hook includes refetch function for manual refresh
    Authorization header included in fetch request
    Types exported for AdminUser interface
  </done>
</task>

### Task 9: Create UserListTable component

<task type="auto">
  <name>Task 9: Create UserListTable component</name>
  <files>
    client/src/components/admin/UserListTable.tsx
  </files>
  <action>
    Create table component for displaying user list with status badges and actions:

    1. Create client/src/components/admin/UserListTable.tsx

    2. Props interface:
       - users: AdminUser[]
       - loading: boolean
       - onRefresh: () => void

    3. Component renders Table component from @/components/ui/table:
       - TableHeader with columns: Usuário, Status, Acesso Manual, Acesso Pago, Ações
       - TableBody mapping over users or showing "Nenhum usuário encontrado" if empty
       - Loading skeleton if loading=true

    4. Each TableRow displays:
       - Email in Usuário column
       - UserStatusBadge in Status column
       - Badge with "Sim/Não" for manual access (show reason if exists)
       - Badge with "Ativo/Expirado" for payment access
       - Action buttons in Ações column:
         * GrantAccessButton (opens dialog)
         * RevokeConfirmDialog (if has access)

    5. Handle empty state with centered message

    6. Handle actions:
       - onGrant: opens grant access dialog with user email pre-filled
       - onRevoke: calls DELETE /api/admin/access/:userId
       - Refresh user list after successful operations

    Import Table components, UserStatusBadge, RevokeConfirmDialog, Button, Badge.
  </action>
  <verify>
    <automated>grep -r "UserListTable" client/src/components/admin/UserListTable.tsx | head -3</automated>
  </verify>
  <done>
    UserListTable component renders user list in table format
    Status badges display with correct colors
    Action buttons for grant/revoke operations
    Empty state and loading state handled
    Component exported correctly
  </done>
</task>

### Task 10: Create validation schemas for admin operations

<task type="auto">
  <name>Task 10: Create validation schemas for admin operations</name>
  <files>
    client/src/lib/validation/admin.ts
  </files>
  <action>
    Create Zod validation schemas for admin form operations:

    1. Create client/src/lib/validation/admin.ts

    2. Export grantAccessSchema:
       - email: z.string().email("Email inválido")
       - reason: z.string().optional()
       - expires_at: z.string().datetime().nullable().optional()

    3. Export revokeAccessSchema:
       - reason: z.string().optional()

    4. Export type definitions:
       - GrantAccessInput = z.infer<typeof grantAccessSchema>
       - RevokeAccessInput = z.infer<typeof revokeAccessSchema>

    These schemas will be used both on client (React Hook Form) and server (route validation).

    Import z from "zod".
  </action>
  <verify>
    <automated>grep -r "grantAccessSchema" client/src/lib/validation/admin.ts | head -3</automated>
  </verify>
  <done>
    grantAccessSchema created with email validation
    revokeAccessSchema created for optional reason
    TypeScript types exported via z.infer
    Schemas match server-side validation
  </done>
</task>

### Task 11: Create AdminPanel page

<task type="auto">
  <name>Task 11: Create main AdminPanel page</name>
  <files>
    client/src/pages/AdminPanel.tsx
  </files>
  <action>
    Create comprehensive admin panel page that integrates all components:

    1. Rename/enhance existing client/src/pages/AdminAccess.tsx to AdminPanel.tsx

    2. Component structure:
       - Verify admin role on mount (redirect to /guia if not admin)
       - Header with title "Painel Administrativo" and refresh button
       - Grant Access section (form with email, reason, expires_at)
       - User List section (UserListTable with all users)

    3. State management:
       - useAdminUsers() for user list
       - Local state for grant form (email, reason, expires_at)
       - Loading states for form submission

    4. Grant access form:
       - Use React Hook Form with grantAccessSchema validation
       - Email input (required)
       - Reason textarea (optional)
       - Expiration date input (optional, date picker)
       - Submit button with loading state
       - Success toast on grant, refresh user list
       - Reset form after successful grant

    5. User list table:
       - Pass users, loading, onRefresh from useAdminUsers
       - Handle grant action (pre-fill email in grant form)
       - Handle revoke action (show confirmation dialog, then call API)

    6. Styling:
       - Use existing color scheme from AdminAccess.tsx
       - Consistent with project design tokens (#12110d background, #d39e17 accent)

    7. Error handling:
       - Show error toasts for failed operations
       - Display validation errors from React Hook Form

    Import useAuth, useAdminUsers, UserListTable, RevokeConfirmDialog, GrantAccessDialog (if created separately), form components, toast from sonner.

    Follow existing AdminAccess.tsx structure but enhance with user listing and comprehensive audit trail.
  </action>
  <verify>
    <automated>grep -r "AdminPanel" client/src/pages/AdminPanel.tsx | head -3</automated>
  </verify>
  <done>
    AdminPanel page displays user list with status badges
    Grant access form with validation working
    Revoke confirmation dialog appears before destructive actions
    Page redirects non-admin users to /guia
    All operations show toast notifications
    Page integrates all admin components
  </done>
</task>

### Task 12: Register new API routes in server

<task type="auto">
  <name>Task 12: Register admin-users router in server</name>
  <files>
    server/index.ts
  </files>
  <action>
    Register the new admin-users router in the main Express app:

    1. Open server/index.ts

    2. Import adminUsersRouter: import { adminUsersRouter } from "./routes/admin-users"

    3. Mount router: app.use("/api/admin/users", adminUsersRouter)

    Verify that adminAccessRouter is already registered (should be at /api/admin/access).

    Ensure both routers are mounted with proper path prefixes.
  </action>
  <verify>
    <automated>grep -r "admin-users" server/index.ts | head -3</automated>
  </verify>
  <done>
    adminUsersRouter imported into server/index.ts
    Router mounted at /api/admin/users
    Server can be started without import errors
  </done>
</task>

### Task 13: Update client routing for AdminPanel

<task type="auto">
  <name>Task 13: Update client routing for AdminPanel</name>
  <files>
    client/src/main.tsx
  </files>
  <action>
    Update client routing to use the new AdminPanel component:

    1. Open client/src/main.tsx

    2. Find the route for /admin or /admin/access

    3. Update import: import AdminPanel from "@/pages/AdminPanel"

    4. Update route component to use AdminPanel instead of AdminAccess

    5. Ensure route is protected (only accessible to admin users)

    Verify the route path matches the admin panel URL.
  </action>
  <verify>
    <automated>grep -r "AdminPanel" client/src/main.tsx | head -3</automated>
  </verify>
  <done>
    AdminPanel component imported in main.tsx
    Route updated to use AdminPanel component
    Admin route accessible at /admin or /admin/access
  </done>
</task>

## Verification

### Overall Phase Checks

1. **Test Suite**: Run `pnpm test` - all tests passing
   - UserStatusBadge component tests pass
   - RevokeConfirmDialog component tests pass
   - admin-access API integration tests pass
   - admin-users API integration tests pass

2. **E2E Tests**: Run `playwright test admin-panel.spec.ts` - all scenarios pass
   - Admin can view user list
   - Status badges show correct colors
   - Grant access flow works
   - Revoke access flow works with confirmation

3. **Manual Verification**:
   - Visit `/admin` route (should redirect non-admin users)
   - Login as admin user (forato@gmail.com or any user with role=admin)
   - Verify user list displays with color-coded badges
   - Test granting access with email, reason, and expiration
   - Test revoking access with confirmation dialog
   - Check database for audit trail entries in admin_audit_log table
   - Verify soft delete pattern (is_active: false, revoked_at, revoked_by populated)

### Database Verification

```sql
-- Check audit log entries
SELECT * FROM admin_audit_log ORDER BY timestamp DESC LIMIT 10;

-- Check soft delete pattern
SELECT * FROM user_manual_access WHERE is_active = false;

-- Verify admin users can read tables
-- Login as admin and check RLS policies
```

### Security Verification

- Test that non-admin users receive 403 when accessing `/api/admin/users`
- Test that requests without Bearer token receive 401
- Test that user_metadata.role check happens on server (not client)
- Verify audit trail records admin_user_id for all operations

## Success Criteria

1. ✅ Admin can view list of all users with color-coded status badges (green=active, yellow=pending, red=expired, blue=manual)
2. ✅ Admin can grant manual access to any user with optional reason and expiration date
3. ✅ Admin can revoke access (manual or paid) with confirmation dialog before destructive action
4. ✅ System records every grant/revoke operation with timestamp, admin user ID, and reason
5. ✅ Admin operations validate admin role on server (service role) before allowing changes
6. ✅ All tests passing (unit, integration, E2E)
7. ✅ Audit trail preserved in database with soft delete pattern

## Output

After completion, create `.planning/phases/03-admin-panel-core/03-01-SUMMARY.md` documenting:
- Components created with file locations
- API endpoints with routes and methods
- Audit trail implementation details
- Test coverage summary
- Any deviations from research or issues encountered
